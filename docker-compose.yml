services:
  publish-web:
    build: .
    container_name: publish-web
    restart: unless-stopped
    ports:
      - "33222:33222"
    volumes:
      # 持久化发布记录，避免容器重建后历史丢失
      - ./publish-records.json:/app/publish-records.json
